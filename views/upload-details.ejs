<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Details - Bulk Email Verifier</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 1000px; margin: 0 auto; padding: 20px; }
        .stats { display: flex; gap: 20px; margin-bottom: 30px; }
        .stat-card { flex: 1; background: #f5f5f5; padding: 20px; border-radius: 5px; text-align: center; }
        .stat-value { font-size: 24px; font-weight: bold; margin: 10px 0; }
        .total { color: #333; }
        .verified { color: #28a745; }
        .invalid { color: #dc3545; }
        .pending { color: #ffc107; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #f2f2f2; }
        .status-badge { padding: 5px 10px; border-radius: 3px; font-size: 12px; }
        .pending { background: #fff3cd; color: #856404; }
        .verified { background: #d4edda; color: #155724; }
        .invalid { background: #f8d7da; color: #721c24; }
        .back-link { display: inline-block; margin-bottom: 20px; }
    </style>
</head>
<body>
    <a href="/upload" class="back-link">← Back to Uploads</a>
    <h1>Upload Details: <%= upload.filename %></h1>
    
    <div class="stats">
        <div class="stat-card">
            <div>Total Emails</div>
            <div class="stat-value total"><%= stats.total %></div>
        </div>
        <div class="stat-card">
            <div>Verified</div>
            <div class="stat-value verified"><%= stats.verified %></div>
        </div>
        <div class="stat-card">
            <div>Invalid</div>
            <div class="stat-value invalid"><%= stats.invalid %></div>
        </div>
        <div class="stat-card">
            <div>Pending</div>
            <div class="stat-value pending"><%= stats.pending %></div>
        </div>
    </div>

    <h2>Email List</h2>
    <% if (emails.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Verified At</th>
                </tr>
            </thead>
            <tbody>
                <% emails.forEach(email => { %>
                    <tr>
                        <td><%= email.email %></td>
                        <td>
                            <span class="status-badge <%= email.status %>">
                                <%= email.status %>
                            </span>
                        </td>
                        <td><%= email.verifiedAt ? email.verifiedAt.toLocaleString() : 'N/A' %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p>No emails found for this upload.</p>
    <% } %>
</body>
</html>